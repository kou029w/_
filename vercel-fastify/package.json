{
  "name": "@kou029w/vercel-fastify",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": ":",
    "vercel-build": "tsup src --clean",
    "preview": "vercel dev",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "fastify": "^3.29.4",
    "fastify-autoload": "^3.12.0",
    "pino-pretty": "^7.6.1"
  },
  "devDependencies": {
    "tsup": "^5.12.6",
    "vercel": "^24.1.0"
  }
}
